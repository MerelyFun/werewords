# 项目记忆

## 用户明确要求

- 调查狼人真言，初始化 Git 项目，生成网页开发计划。
- 希望后续协助发布到 GitHub Pages；本轮未要求立即上线。
- 开始任务考虑并使用有价值的并行子代理，避免闲置代理占用资源。

## 已确认

- 工作目录：E:\Code\Wolf，初始为空目录。
- 游戏为 Werewords，包含猜词与隐藏身份。
- 用户已明确：线下共用一部手机，已有实体身份卡，不需要数字发牌、传手机查看身份或在线多人房间。
- GitHub Pages 是静态托管平台，可以托管此前端。
- 用户要求每阶段可跳过，夜晚所有人闭眼需要语音播报；最初要求模型生成，随后提供自己的 MP3 并要求裁剪接入。
- 当前采用用户提供的 `E:/年会/串烧/狼人真言音频.MP3`（49.872 秒），原件保存为 `assets/audio/host-source.mp3`，按 `docs/audio-cuts.json` 裁为 13 段。无需 API 密钥，不再等待密钥配置；OpenAI 生成脚本仅为可选工具。

## 已采用的实现

- 首版采用 4–10 人基础角色范围。
- Vite + React + TypeScript 静态前端，无后端。
- 身份分配和揭牌在线下完成；网页不维护玩家身份映射，镇长按实体卡身份行动。
- 手机放在方便夜晚相关角色看词的位置，分阶段显示与隐藏词语；白天由镇长操作。结算由玩家根据实体卡确认结果。

## 2026-09-08：圆桌界面与角色配置

- 用户要求提示同时提供正向与反向，方便围坐一圈；移除“语音主持已就绪”等冗余界面信息。
- 闭眼等待默认 4 秒；看词独立设置，默认仍为 8 秒。
- 用户要求阶段配图，并在开局增加可配置角色；新增角色应通过统一目录方便维护，不散落在页面条件判断中。
- 角色目录为 `src/roles.ts`。新增爪牙、观察者、共济会成员、怪物；村民自动补齐剩余人数，共济会成员成对选择。镇长为额外公开职务，不占第二个人数名额。
- “选角色”指配置实体卡组合，不是选择玩家身份。仍无发牌、玩家名单或身份映射。
- 用户明确扩展角色配音“后面再说，先留着”：基础录音保留；缺少扩展录音的组合整局使用手动预演，不自动推进夜晚。不得将扩展自动语音记为完成。

## 发布待补信息

- GitHub 账号、目标仓库名称或已有仓库地址、可用登录授权。
- 公开仓库还是私有仓库；私有仓库的 Pages 可用性取决于 GitHub 套餐。

## 2026-09-09 界面修正

- 镇长选词仅单向显示。先知/狼人看词保留双向大字答案，去掉标题、说明、插图和倒计时，仅留必要图标控制。
- 字体统一使用本机中文无衬线字体栈，不依赖外部字体下载。

## 2026-09-09 发布授权

用户明确要求部署到 GitHub 并公开访问。使用已登录账号 MerelyFun，新建公开仓库 werewords；origin=https://github.com/MerelyFun/werewords.git，Pages目标 https://merelyfun.github.io/werewords/。已启用 GitHub Actions 发布来源和 HTTPS。

## 2026-09-09 封面和词库要求

用户要求高质感图片封面、配置角色配图片、词库按难度选。外部词库必须先调查让用户选择，再导入；本轮保持原有128词不变，候选为日常聚会/动漫游戏与老梗/成语挑战。

## 2026-09-09：用户选定两份 Steam 词库

用户已明确要求尝试加入工坊3022451195（你画我歪：乱七八糟啥都有）与3416324742（阴间词汇大合集）。此两份不再等待选库确认；需先获取真实附件再统计、去重、分难度接入，不可用标题词数或自编替代词冒充导入结果。

## 2026-09-09：群友词库选择

用户已选择接入群友派对之夜，并要求按词库来源选择出词范围。已实现300词、三级难度与来源保存；进一步候选仍需先让用户选，见docs/NEW_WORD_CANDIDATES.md。
