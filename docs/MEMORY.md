# 项目记忆

## 用户明确要求

- 调查狼人真言，初始化 Git 项目，生成网页开发计划。
- 希望后续协助发布到 GitHub Pages；本轮未要求立即上线。
- 开始任务考虑并使用有价值的并行子代理，避免闲置代理占用资源。

## 已确认

- 工作目录：E:\Code\Wolf，初始为空目录。
- 游戏为 Werewords，包含猜词与隐藏身份。
- 用户已明确：线下共用一部手机，已有实体身份卡，不需要数字发牌、传手机查看身份或在线多人房间。
- GitHub Pages 是静态托管平台，可以托管此前端。
- 用户要求每阶段可跳过，夜晚所有人闭眼需要语音播报；最初要求模型生成，随后提供自己的 MP3 并要求裁剪接入。
- 当前采用用户提供的 `E:/年会/串烧/狼人真言音频.MP3`（49.872 秒），原件保存为 `assets/audio/host-source.mp3`，按 `docs/audio-cuts.json` 裁为 13 段。无需 API 密钥，不再等待密钥配置；OpenAI 生成脚本仅为可选工具。

## 已采用的实现

- 首版采用 4–10 人基础角色范围。
- Vite + React + TypeScript 静态前端，无后端。
- 身份分配和揭牌在线下完成；网页不维护玩家身份映射，镇长按实体卡身份行动。
- 手机放在方便夜晚相关角色看词的位置，分阶段显示与隐藏词语；白天由镇长操作。结算由玩家根据实体卡确认结果。

## 2026-09-08：圆桌界面与角色配置

- 用户要求提示同时提供正向与反向，方便围坐一圈；移除“语音主持已就绪”等冗余界面信息。
- 闭眼等待默认 4 秒；看词独立设置，默认仍为 8 秒。
- 用户要求阶段配图，并在开局增加可配置角色；新增角色应通过统一目录方便维护，不散落在页面条件判断中。
- 角色目录为 `src/roles.ts`。新增爪牙、观察者、共济会成员、怪物；村民自动补齐剩余人数，共济会成员成对选择。镇长为额外公开职务，不占第二个人数名额。
- “选角色”指配置实体卡组合，不是选择玩家身份。仍无发牌、玩家名单或身份映射。
- 用户明确扩展角色配音“后面再说，先留着”：基础录音保留；缺少扩展录音的组合整局使用手动预演，不自动推进夜晚。不得将扩展自动语音记为完成。

## 发布待补信息

- GitHub 账号、目标仓库名称或已有仓库地址、可用登录授权。
- 公开仓库还是私有仓库；私有仓库的 Pages 可用性取决于 GitHub 套餐。

## 2026-09-09 界面修正

- 镇长选词仅单向显示。先知/狼人看词保留双向大字答案，去掉标题、说明、插图和倒计时，仅留必要图标控制。
- 字体统一使用本机中文无衬线字体栈，不依赖外部字体下载。

## 2026-09-09 发布授权

用户明确要求部署到 GitHub 并公开访问。使用已登录账号 MerelyFun，新建公开仓库 werewords；origin=https://github.com/MerelyFun/werewords.git，Pages目标 https://merelyfun.github.io/werewords/。已启用 GitHub Actions 发布来源和 HTTPS。

## 2026-09-09 封面和词库要求

用户要求高质感图片封面、配置角色配图片、词库按难度选。外部词库必须先调查让用户选择，再导入；本轮保持原有128词不变，候选为日常聚会/动漫游戏与老梗/成语挑战。

## 2026-09-09：用户选定两份 Steam 词库

用户已明确要求尝试加入工坊3022451195（你画我歪：乱七八糟啥都有）与3416324742（阴间词汇大合集）。此两份不再等待选库确认；需先获取真实附件再统计、去重、分难度接入，不可用标题词数或自编替代词冒充导入结果。

## 2026-09-09：群友词库选择

用户已选择接入群友派对之夜，并要求按词库来源选择出词范围。已实现300词、三级难度与来源保存；进一步候选仍需先让用户选，见docs/NEW_WORD_CANDIDATES.md。


## 2026-09-09 首页与多选词库调整

- 首页采用整幅月夜封面、香槟金主按钮、开放式设置列表；看词与闭眼时间收进“更多设置”，角色图保留。
- 词库按名称多选，不显示词数、不提供类型/主题分类；至少选一个库，混抽按词面去重。
- 难度统一标准/挑战，标准合并原简单与中等；兼容旧单库设置并忽略旧分类。
- 验证：构建、42项单元测试、20项Chrome浏览器测试通过，320px无横向溢出；390px截图已检查。内置浏览器初次连接失败，使用真实Chrome完成验证。
- 白天页面已合并，最终构建与23项浏览器测试全通过；扩展角色配音仍待补充。

## 2026-09-09：白天单屏游戏台

- 用户确认上下双向仅用于夜晚；镇长选词仍单向，天亮、猜词、指认、讨论、人工确认与结果均使用单屏。
- 白天保留已有原创角色插图，放大居中展示，以大号倒计时、细进度线和克制金色按钮为重点；不新增数字身份或发牌。
- 本轮与封面任务协调，只修改游戏阶段渲染，独立样式位于 src/day-stage.css。

## 2026-09-09：新增两库与来源防重要求

用户指定加入 Steam 2660283448（笑死人不偿命329词）及2890545389（乱七八糟你画我歪462词），并要求记录每库来源、避免重复。工坊ID是来源唯一键；相近标题不可合并，重复导入同ID应更新原记录而非追加。来源注册表为src/data/steam-sources.json，原附件按ID保存在assets/wordlists/steam/。跨库重名保留来源，但混抽按归一化词面去重。

同时将此前Steam订阅榜、最新发布且订阅>100榜、最近更新且订阅>10000榜各扩到前10，只导入用户指定两份，不默认导入榜单其他候选。


## 2026-09-09：词库语言限制

用户本轮明确GitHub词库只要中文，要求Star前10及Star>50的最新前10；继续限定猜词游戏出题数据。不可再混入外语库、NLP或输入法词表。最新暂沿用题库所在文件最后提交口径，需区分代码修改与实质词条更新。


## 2026-09-09：汉兜与Parti已授权接入

用户明确选定汉兜handle和Parti，不再等待选库确认。来源ID为github-handle/github-parti，固定版本和许可见src/data/github-sources.json。保留424/880条中文原词，标准/挑战由本项目整理；Parti非商业许可应持续保留，不改标为MIT。

## 2026-09-09：自编综合词库授权

用户要求自行生成几百词，覆盖各平台近年热词、古今中外人物景物、常见词与有趣梗，避免太偏，并适配狼人真言的好猜／难猜体验。已按本要求自编万象趣猜660词（标准430／挑战230），独立来源generated-wanxiang；不需等待外部选库确认。分类仅供编辑，不新增首页主题筛选。


## 2026-09-09：词库下拉收纳

用户要求词库列表可下拉，避免过长。保持多选，默认收起，摘要显示首个已选名称及来源数量；展开后长列表在框内滚动。

## 2026-09-09：暂不分难度

用户要求暂时不分标准／挑战。首页仅选词库，所选来源全部词条混抽；旧难度设置忽略，统一归一化为all。保留词条历史分级，方便以后恢复，当前不参与游戏筛选。

## 2026-09-09：镇长候选词数

用户要求在更多设置中配置每次镇长从几个词里选。已增加2–6个候选、默认3个，沿用本地保存；每轮从已选词库全池按词面去重抽取。
