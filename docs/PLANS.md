# 网页开发计划

状态：阶段 1、2 已完成；阶段 3 已完成用户录音裁剪接入及桌面真实音频验证，手机试听待办。阶段 4 尚未开始。日期：2026-09-08。

## 产品方向

用户已确认：线下共用一部手机。首版建议 4–10 人基础角色，自编中文词库，无账号。所有阶段均可跳过，夜晚语音为必要功能。用户已有实体身份卡，发牌和查看身份在线下进行。网页引导镇长选词、夜晚相关角色看词，白天由镇长操作计时与猜词结果。

## 阶段 0：准备（本轮）

- [x] 调查游戏及官方资料，记录规则版本差异。
- [x] 初始化本地 Git main 分支。
- [x] 保存计划、项目记忆和进度。
- [x] 用户确定网页用途：线下共用一部手机。

## 阶段 1：规则规格与可运行骨架

目标：固定一个规则版本，建立最小可运行页面。

- 将人数配置、镇长隐藏身份、夜晚信息、回答资源、计时和胜负整理成规则表。
- 身份卡在线下分发；网页不录入玩家身份名单、不随机分配身份。镇长根据实体卡按对应夜晚提示行动，不要求在网页再次录入身份。
- 已采用 Vite + React + TypeScript，并提交依赖锁文件。
- 设计首页、开局设置、选词与夜晚引导、游戏台、结算五类屏幕。
- 原创中文视觉，移动端大按钮，暗色适合聚会但保持文字对比度。

验收：页面可启动；规则表覆盖镇长为村民、狼人、先知及多狼人情况；构建与类型检查通过。一次提交完成骨架。

## 阶段 2：打通一局

- 设置人数、难度和时长；首版固定基础角色，用于引导与计时，不要求输入玩家名单。
- 镇长选词，按夜晚阶段引导先知、狼人看词并及时隐藏；不要求传手机查看身份。
- 自编小型中文词库，含分类与难度，随机选词避免一局中重复候选。
- 游戏台显示倒计时与规则所需回答标记；镇长手动确认猜中。
- 猜中进入狼人指认计时，未猜中或资源耗尽进入讨论投票计时；玩家在线下指认、投票、揭牌，再在网页确认是否找到先知或狼人并结算。
- 显式管理状态转换，结算后可重新开局并清空上一局秘密。

验收：人工走通猜中/未猜中两条完整路径；验证指认成功和失败、多人狼人、平票、镇长特殊身份。状态机与根据人工输入结果得出胜负的逻辑用针对性单元测试验证，不依赖玩家身份数据。

## 阶段 3：体验完善与验证

- 本地保存人数、难度等设置；不维护玩家身份数据，不持久化当局词语。
- 倒计时以截止时间计算，浏览器切后台回来校正；刷新时明确重新开局策略。
- 防止误触泄密，确认重新开局，提供规则帮助。
- 夜晚声音引导为必要功能：将用户提供的 49.872 秒 MP3 裁为 13 段静态音频，原件位于 `assets/audio/host-source.mp3`，切点位于 `docs/audio-cuts.json`。使用 `npm run voice:cut` 和 `npm run voice:check`，无需 API 密钥；OpenAI 生成脚本仅保留为可选工具。无音频时明确提供无声预览，不自动推进夜晚。
- 桌面与移动端浏览器检查布局、控制台错误、触屏操作和可访问性。

验收：窄屏无横向溢出；非看词阶段不意外显示答案；后台计时正确；无阻断性控制台错误。运行项目实际提供的构建、类型检查及规则测试。

## 阶段 4：发布 GitHub Pages

前提：网页构建和玩法验证通过，并取得目标 GitHub 账号/仓库与可用授权。

1. 创建或连接用户指定的 GitHub 仓库，设置 remote 并推送。
2. 配置仓库子路径和静态路由策略，确保直接打开或刷新页面不 404。
3. 添加 GitHub Actions 工作流：安装锁定依赖、检查、构建、上传 dist、部署 Pages；实现时核对官方 action 版本与权限。
4. 将 Pages 发布来源设为 GitHub Actions，检查首次运行日志。
5. 打开正式网址，验证资源、移动端及完整一局，再交付链接。

验收：工作流成功，线上可访问，子路径资源正常，README 写明实际访问地址与更新方法。当前不创建空站发布工作流。

## 可选后续

自定义词库导入、其余豪华版角色、离线缓存。在线多人房间不在当前范围。

## 本轮追加：圆桌界面与配置

- 正反双向显示阶段提示与秘密词，保留暂停遮挡；精简首页与阶段中的冗余状态信息。
- 闭眼等待默认 4 秒，看词默认 8 秒，两者独立配置。
- 已添加原创阶段角色插图 `public/images/roles.png`，来源记录见 `docs/design/roles.md`。
- 以 `src/roles.ts` 统一定义角色信息、数量规则与夜晚步骤；增加爪牙、观察者、共济会成员、怪物，村民补位，镇长为额外职务。
- 扩展配音按用户要求后续再补。当前选择缺音频角色时明确提供整局手动预演，禁止无声自动推进夜晚。

验收范围：角色数量归一化、成对角色、阶段顺序、非看词角色不泄词、4 秒闭眼与 8 秒看词、双向提示及精简页面。本轮单元 30 项、Chrome 端到端 15 项通过，构建通过，原有 13 段录音检查通过。扩展配音与真实手机试玩仍待后续。

## 下一步

本轮裁剪资源检查、原生 WebAudio 解码与播放验证已通过；完成真实手机试听后结束阶段 3；随后按用户指定 GitHub 仓库推进阶段 4。当前不再等待 API 密钥。

## 2026-09-09 发布完成

阶段4已完成：公开仓库 MerelyFun/werewords、main自动发布、Pages HTTPS、正式网址资源与游戏交互验收均通过。网址 https://merelyfun.github.io/werewords/ 。下一步仅真实手机现场试玩与用户后续要求的扩展配音。


## 2026-09-09 首页与多选词库调整

- 首页采用整幅月夜封面、香槟金主按钮、开放式设置列表；看词与闭眼时间收进“更多设置”，角色图保留。
- 词库按名称多选，不显示词数、不提供类型/主题分类；至少选一个库，混抽按词面去重。
- 难度统一标准/挑战，标准合并原简单与中等；兼容旧单库设置并忽略旧分类。
- 验证：构建、42项单元测试、20项Chrome浏览器测试通过，320px无横向溢出；390px截图已检查。内置浏览器初次连接失败，使用真实Chrome完成验证。
- 白天页面已合并，最终构建与23项浏览器测试全通过；扩展角色配音仍待补充。

## 2026-09-09：单屏阶段验收

- [x] 双向展示限定夜晚（镇长选词例外，维持单向）；从天亮起全部单屏。
- [x] 白天图片与大计时视觉调整，回答、暂停、跳过与人工结算保留。
- [x] 手机窄屏及横屏布局、夜晚遮挡、计时与结算回归通过，证据见PROGRESS。
- [ ] 与封面调整统一发布后的正式网址验收。
